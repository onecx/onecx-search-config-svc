app:
  name: svc
  image:
    repository: "onecx/onecx-search-config-svc"
  db:
    enabled: true
  operator:
    keycloak:
      client:
        enabled: true
        spec:
          kcConfig:
            defaultClientScopes: [ ocx:tenant:read ]
